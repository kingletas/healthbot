# Copy to backend.hcl and fill in. backend.hcl is gitignored: it names your
# state bucket, which is not this repository's business.
bucket = "example-terraform-state"
key    = "stage/stacks/healthbot/terraform.tfstate"
region = "us-east-2"
