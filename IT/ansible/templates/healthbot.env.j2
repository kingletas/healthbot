# {{ ansible_managed }}
# Non-secret runtime configuration for the healthbot systemd unit.
# Secrets live in Secrets Manager; this file only points at them.
{% for key, value in healthbot_env.items() %}
{{ key }}="{{ value }}"
{% endfor %}
