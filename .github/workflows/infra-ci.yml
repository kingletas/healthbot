---
name: Infra CI

on:
  push:
    branches: [main]
    paths:
      - "IT/**"
      - ".github/workflows/infra-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "IT/**"
      - ".github/workflows/infra-ci.yml"

permissions:
  contents: read

# One run per branch. A push that supersedes another cancels it rather than
# paying for an answer nobody will read, since these are metered while the
# repository is private.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  terraform:
    name: terraform fmt · validate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./IT/terraform
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.10"

      # No credentials in CI: validate against the providers only, never the
      # backend or live state
      - name: Terraform Init (no backend)
        run: terraform init -backend=false -input=false

      - name: Terraform Format
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        run: terraform validate

  packer:
    name: packer fmt · validate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./IT/packer
    steps:
      - uses: actions/checkout@v4

      - name: Setup Packer
        uses: hashicorp/setup-packer@v3

      - name: Packer Format
        run: packer fmt -check .

      - name: Packer Init
        run: packer init .

      # etc/example.hcl is tracked precisely so this needs no credentials and
      # no var file of anybody's own.
      - name: Packer Validate
        run: packer validate -var-file=etc/example.hcl .

  tfsec:
    name: tfsec
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tfsec
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: IT/terraform
          soft_fail: true

  ansible:
    name: ansible-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Sync dependencies from uv.lock
        run: uv sync --locked

      - name: Install the collections the playbook uses
        run: uv run ansible-galaxy collection install -r IT/ansible/requirements.yml

      - name: Lint the playbook
        run: uv run ansible-lint IT/ansible/
